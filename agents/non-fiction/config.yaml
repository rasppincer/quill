# Non-Fiction Agent Set
# Configurable LLM-powered agents for non-fiction writing pipeline.
# Model/API config lives in agents/model.yaml (global).

description: "Non-fiction agents for blog posts, essays, articles, and analysis"
temperature: 0.7
max_tokens: 4096
max_loops: 3
trigger: "on_advance"  # on_advance | auto

stages:
  outline:
    name: "Outline Agent"
    description: "Creates structured outline from brief with thesis and evidence hooks"
    temperature: 0.6

  draft:
    name: "Draft Agent"
    description: "Produces full evidence-backed draft from outline"
    temperature: 0.7

  review:
    name: "Review Agent"
    description: "Reads draft and produces structural/quality critique"
    temperature: 0.5

  revise:
    name: "Revise Agent"
    description: "Applies review feedback to produce revised draft"
    temperature: 0.6

  humanize:
    name: "Humanize Agent"
    description: "Strips AI patterns, scores human-ness"
    temperature: 0.4

  validate:
    name: "Validate Agent"
    description: "Checks factual accuracy, sources, statistics, and claims"
    temperature: 0.2

  polish:
    name: "Polish Agent"
    description: "Final line-level edits, readability scoring, SEO optimization"
    temperature: 0.3
